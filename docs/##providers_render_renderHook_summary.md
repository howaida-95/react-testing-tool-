- wrapper options for providers ==> wrapper for each component in the test
- custom render function => custom wrapper contain provider
- test custom react hook => using renderHook
- act utility
  act is a utility that makes sure all React updates (state updates, effects, re-renders) caused by an action are fully processed before you make assertions in your test.
