## getByTestId(value, options)

data-testid(testing identifier) attribute --> to target elements in tests
return the element that has matching data-testid attribute

# options

- exact
- trim
- normalizer
