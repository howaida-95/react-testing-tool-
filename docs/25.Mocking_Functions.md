steps:
- create mock function --> jest.fn()
- pass function as prop to the component 
- make sure btns are there 
- simulate user interaction ==> userEvent.click()
- expect().toHaveBeenCalledTimes() ==> use matcher  