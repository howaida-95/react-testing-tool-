## getByText 
search for all elements that have text node with text content matches given text

## options 
1.exact — whether the match should be exact (true by default).
2.selector — to narrow down which element type to match (e.g. 'input').








