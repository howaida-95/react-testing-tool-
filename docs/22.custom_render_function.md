--> top level wrapper (available to every component)
ex:
theme provider
redux store provider

# => custom render function

create a single wrapper across all tests
save us from specifying the wrapper option in every test

steps:

- create test utils file in src folder
- import everything from react testing library
- import them in test files
